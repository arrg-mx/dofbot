cmake_minimum_required(VERSION 3.0.2)
project(dofbot_bringup)

find_package(catkin REQUIRED)

include_directories(
  ${catkin_INCLUDE_DIRS}
)

# install(DIRECTORY launch
#  DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION}
# )
